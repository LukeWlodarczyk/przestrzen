---
interface Props {
  hasImage: boolean;
}

const { hasImage } = Astro.props;
---

{
  hasImage && (
    <script>
      import initImage from "@components/media/Image/script/image"; initImage();
    </script>
  )
}
